<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="16dp"
    android:background="@android:color/white"
    tools:context=".SymptomCheckerPage">

    <!-- Title -->
    <TextView
        android:id="@+id/titleText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="🐾 Pet Illness Detection"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="12dp" />

    <!-- Pet Image Preview -->
    <ImageView
        android:id="@+id/petImage"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:src="@drawable/placeholder"
        android:layout_marginTop="12dp"
        android:background="@android:color/darker_gray"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toBottomOf="@id/titleText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Upload from Gallery Button -->
    <Button
        android:id="@+id/btnUploadImage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="📁 Upload from Gallery"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/petImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Open Camera Button -->
    <Button
        android:id="@+id/btnOpenCamera"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="📷 Take Photo"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/btnUploadImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Symptom Input -->
    <EditText
        android:id="@+id/symptomInput"
        android:layout_width="0dp"
        android:layout_height="120dp"
        android:hint="Describe your pet's symptoms..."
        android:background="@android:drawable/editbox_background"
        android:gravity="start|top"
        android:textColor="@android:color/black"
        android:padding="12dp"
        android:inputType="textMultiLine"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/btnOpenCamera"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Analyze Button -->
    <Button
        android:id="@+id/btnAnalyze"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="🩺 Analyze Illness"
        android:textStyle="bold"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/symptomInput"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Scrollable Diagnosis Output -->
    <ScrollView
        android:id="@+id/resultScroll"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/btnAnalyze"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/resultText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="🧠 Diagnosis result will be shown here."
            android:textSize="16sp"
            android:textColor="@android:color/black"
            android:padding="12dp" />
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
