<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:openDrawer="start">

    <!-- Main content -->
    <RelativeLayout
        android:id="@+id/dashboardLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FDF5E6">

        <!-- Custom Toolbar -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#FDF5E6"
            android:elevation="0dp">

            <!-- Custom toolbar content using ConstraintLayout for better control -->
            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingEnd="16dp">

                <!-- Hamburger Menu Button -->
                <ImageButton
                    android:id="@+id/btnMenu"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_menu"
                    android:contentDescription="Menu"
                    android:scaleType="centerInside"
                    android:padding="8dp"
                    app:tint="#8D6E63"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent" />

                <!-- PawLife Title with bone icon - Fixed width to prevent collapse -->
                <LinearLayout
                    android:id="@+id/titleLayout"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:layout_marginStart="12dp"
                    android:layout_marginEnd="8dp"
                    app:layout_constraintStart_toEndOf="@id/btnMenu"
                    app:layout_constraintEnd_toStartOf="@id/petProfilesContainer"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintHorizontal_bias="0"
                    app:layout_constraintWidth_min="120dp">

                    <ImageView
                        android:layout_width="24dp"
                        android:layout_height="24dp"
                        android:layout_marginEnd="8dp"
                        app:tint="#F4D03F" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="PawLife"
                        android:textSize="24sp"
                        android:textStyle="bold"
                        android:textColor="#8D6E63"
                        android:maxLines="1"
                        android:ellipsize="none" />

                </LinearLayout>

                <!-- Pet profiles container with fixed width -->
                <LinearLayout
                    android:id="@+id/petProfilesContainer"
                    android:layout_width="200dp"
                    android:layout_height="65dp"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:paddingTop="7dp"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent">

                    <!-- Container for pet profiles with scroll indicator -->
                    <FrameLayout
                        android:layout_width="0dp"
                        android:layout_height="match_parent"
                        android:layout_weight="1"
                        android:layout_marginEnd="8dp">

                        <!-- Horizontal RecyclerView for pet profiles -->
                        <androidx.recyclerview.widget.RecyclerView
                            android:id="@+id/recyclerViewPetProfiles"
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:orientation="horizontal"
                            android:clipToPadding="false"
                            android:paddingStart="4dp"
                            android:paddingEnd="4dp"
                            android:scrollbars="none"
                            android:overScrollMode="never"
                            tools:listitem="@layout/item_pet_profile" />

                        <!-- Scroll indicator (optional - appears when scrollable) -->
                        <LinearLayout
                            android:id="@+id/scrollIndicator"
                            android:layout_width="match_parent"
                            android:layout_height="3dp"
                            android:layout_gravity="bottom"
                            android:orientation="horizontal"
                            android:layout_marginBottom="2dp"
                            android:visibility="gone">

                            <View
                                android:id="@+id/scrollIndicatorStart"
                                android:layout_width="0dp"
                                android:layout_height="match_parent"
                                android:layout_weight="1"
                                android:background="#E0E0E0"
                                android:layout_marginEnd="1dp" />

                            <View
                                android:id="@+id/scrollIndicatorEnd"
                                android:layout_width="0dp"
                                android:layout_height="match_parent"
                                android:layout_weight="1"
                                android:background="#E0E0E0"
                                android:layout_marginStart="1dp" />

                        </LinearLayout>

                    </FrameLayout>

                    <!-- Add Pet Button Container -->
                    <FrameLayout
                        android:layout_width="56dp"
                        android:layout_height="56dp">

                        <com.google.android.material.floatingactionbutton.FloatingActionButton
                            android:id="@+id/fabAddPet"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_gravity="center"
                            app:fabCustomSize="48dp"
                            app:srcCompat="@drawable/icadd"
                            app:maxImageSize="24dp"
                            app:backgroundTint="#025293"
                            app:tint="@android:color/white"
                            app:borderWidth="0dp"
                            app:elevation="2dp" />

                    </FrameLayout>

                </LinearLayout>

            </androidx.constraintlayout.widget.ConstraintLayout>

        </androidx.appcompat.widget.Toolbar>

        <!-- Scrollable main content -->
        <ScrollView
            android:id="@+id/scrollView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/toolbar"
            android:layout_above="@id/bottomNav"
            android:fillViewport="true">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <!-- Welcome Card -->
                <com.google.android.material.card.MaterialCardView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="20dp"
                    app:cardCornerRadius="20dp"
                    app:cardElevation="4dp"
                    android:backgroundTint="#F6F6F5">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:padding="20dp"
                        android:gravity="center_vertical">

                        <LinearLayout
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_weight="1"
                            android:orientation="vertical">

                            <TextView
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="Hi, homies"
                                android:textSize="20sp"
                                android:textStyle="bold"
                                android:textColor="#8D6E63" />

                            <TextView
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="how are you"
                                android:textSize="18sp"
                                android:textColor="#8D6E63"
                                android:layout_marginTop="4dp" />

                            <TextView
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="today?"
                                android:textSize="18sp"
                                android:textColor="#8D6E63"
                                android:layout_marginTop="4dp" />

                        </LinearLayout>

                        <!-- Cat and Dog Preview Image -->
                        <ImageView
                            android:layout_width="190dp"
                            android:layout_height="120dp"
                            android:layout_marginStart="16dp"
                            android:src="@drawable/catdogpreview"
                            android:scaleType="centerCrop"
                            android:contentDescription="Cat and Dog Preview" />

                    </LinearLayout>

                </com.google.android.material.card.MaterialCardView>

                <!-- Upcoming Events Section -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="UPCOMING EVENT"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:textColor="#8D6E63"
                    android:layout_marginBottom="16dp" />

                <!-- RecyclerView for pet events -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerViewPetEvents"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    tools:listitem="@layout/item_pet" />

            </LinearLayout>

        </ScrollView>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabChatbot"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_above="@id/bottomNav"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            android:src="@drawable/ic_toy_logo"
            app:backgroundTint="#025293"
            app:tint="@null"
            app:elevation="8dp" />

        <!-- Bottom Navigation Bar -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNav"
            android:layout_width="match_parent"
            android:layout_height="70dp"
            android:layout_alignParentBottom="true"
            android:background="#025293"
            app:labelVisibilityMode="labeled"
            app:itemIconTint="@color/white"
            app:itemTextColor="@color/white"
            app:menu="@menu/bottom_nav" />

    </RelativeLayout>

    <!-- Navigation Drawer -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_main"
        app:menu="@menu/nav_drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>